# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: CMake on a single platform

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: sudo apt-get install libpulse-dev

    - name: Build sample
      run: |
        cmake -B ${{github.workspace}}/build_sample -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} sample
        cmake --build ${{github.workspace}}/build_sample --config ${{env.BUILD_TYPE}}

    - name: Build emulator
      run: |
        cmake -B ${{github.workspace}}/build_emulator -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} emulator
        cmake --build ${{github.workspace}}/build_emulator --config ${{env.BUILD_TYPE}}

    - name: Test projects
      working-directory: ${{github.workspace}}/projects
      run: ./build.sh

    - name: decoder-esp build v2
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v6.0
        target: esp32s3
        path: 'decoder-esp32'
        command: './build32M.sh'

    - name: decoder-esp build v3
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v6.0
        target: esp32s3
        path: 'decoder-esp32'
        command: './build32Mv3.sh'
